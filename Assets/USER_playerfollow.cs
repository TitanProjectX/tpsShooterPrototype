using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class USER_playerfollow : MonoBehaviour
{
    // Start is called before the first frame update
  //  public Transform Original_Y;
   // public Transform Copy_Y;
   // public Transform player_head;
   // public Transform player_body;
    void Start()
    {
       // player_head = GameObject.FindGameObjectWithTag("Player").GetComponent<Transform>();     
    }

    // Update is called once per frame
    void Update()
    {
       // Debug.Log(Original_Y.rotation.eulerAngles.y);
        //Copy_Y.transform.rotation = Quaternion.Euler(Copy_Y.transform.rotation.x, Original_Y.rotation.eulerAngles.y, Copy_Y.rotation.z);
      //  player_body.LookAt(player_head.transform.position + player_head.transform.position);
    }
}
