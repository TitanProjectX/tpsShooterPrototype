using UnityEngine;

public class SYSTEM_soundmanager : MonoBehaviour
{
    public AudioSource[] Client_sounds;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
